logging:
  level:
    org.springframework.security: DEBUG

dev:
  bypass:
    enabled: true

app:
  websocket:
    allowed-origin-patterns:
      - "http://localhost:*"
      - "http://127.0.0.1:*"

spring:
  sql:
    init:
      mode: always
      continue-on-error: true
